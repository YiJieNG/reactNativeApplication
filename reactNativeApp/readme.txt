This is a react native application built by Android Studio using Java programming language.

The techniques applied include linking with firebase, view, navigation bar, toast messages, UI, button, layout types, control of landscape and portrait mode and so on.

The application runs on andriod emulator with apk version 30.